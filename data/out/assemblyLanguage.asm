DATAS SEGMENT
	result DD 0
	a DD 0
	b DD 0
	c DD 0
DATAS ENDS

CODES SEGMENT 
ASSUME CS:CODES,DS:DATAS
START: 
	MOV AX,DATAS
	MOV DS,AX
	MOV a,8
	MOV b,5
	MOV EAX,3
	SUB EAX,a
	MOV c,EAX
	MOV EBX,a
	IMUL EBX,b
	MOV ECX,3
	ADD ECX,b
	SUB EAX,a
	IMUL ECX,EAX
	SUB EBX,ECX
	MOV result,EBX
	MOV AH,4CH
	INT 21H
CODES ENDS
	END START

